var config = {
    port: 9000,
    maxRetry: 10,
    logMode: 0,
    view: '{{{staticViews}}}',
    autoOpen:true,
    mock: {
        api: {
            root: '{{{apiMock}}}',
            get: 'get',
            post: 'post',
        },
        proxy: null,
        fileName: 'data',
    },
    template: {
        engine: 'ftl',
        root: '{{{templatesRoot}}}',
        mock: '{{{templatesMock}}}',
    },
    resource: [
        '{{{webRoot}}}'
    ],
    watch: [
        '{{{webRoot}}}/src/css',
        '{{{webRoot}}}/src/stykesheet',
        '{{{webRoot}}}/src/js',
        '{{{webRoot}}}/src/javascript',
    ],
    pages: [
        {{#each pages}}
            {name: '{{{this.name}}}', url: '{{{this.url}}}', path: '.{{{this.path}}}'},
        {{/each}}
    ]

};

module.exports = config;