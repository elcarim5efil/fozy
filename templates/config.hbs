var config = {
    port: 9000,
    maxRetry: 10,
    logMode: 0,
    view: '{{{staticViews}}}',
    autoOpen:true,
    mock: {
        api: {
            root: '{{{asyncMockRoot}}}',
            get: 'get',
            post: 'post',
        },
        proxy: null,
        fileName: 'data',
    },
    template: {
        engine: 'ftl',
        root: '{{{templatesRoot}}}',
        mock: '{{{syncMockRoot}}}',
    },
    resource: [
        '{{{resourceRoot}}}'
    ],
    watch: [
        '{{{resourceRoot}}}/src/css/**/*.css',
        '{{{resourceRoot}}}/src/stylesheet/**/*.css',
        '{{{resourceRoot}}}/src/js/**/*.js',
        '{{{resourceRoot}}}/src/javascript/**/*.js',
        '{{{resourceRoot}}}/src/js/**/*.html',
        '{{{resourceRoot}}}/src/javascript/**/*.html',
    ],
    pages: [
        {{#each pages}}
            {name: '{{{this.name}}}', url: '{{{this.url}}}', path: '.{{{this.path}}}'},
        {{/each}}
    ]

};

module.exports = config;