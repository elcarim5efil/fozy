var config = {
    port: 9000,
    maxRetry: 10,
    logMode: 0,
    view: './views',
    mock: {
        api: './mock/api/',
        proxy: null,
    },
    template: {
        engine: 'ftl',
        root: './templates',
        page: 'pages',
        mock: './mock/ftl',
    },
    resource: [
        './src'
    ],
    watch: [
        './src/css',
        './src/js',
    ],
    pages: [
        {{#each pages}}
            {name: '{{{this.name}}}', url: '{{{this.url}}}', path: '.{{{this.path}}}'},
        {{/each}}
    ]

};

module.exports = config;